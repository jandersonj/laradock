

1 - Após clonar o repositório, entrar na pasta do repositório e rodar do (composer install) para instalar as dependências

2 - Após instaladas as dependências entrar na pasta laradock pelo terminal e rodar o comando - docker-compose up -d nginx mysql phpmyadmin

Rodando as migrations para criação da tabela necessário no banco

1 - Após ter inciado os containers rodar o comando docker-compose exec --user=laradock workspace bash

2 - Entrado no workspace, rodar o comando - (php artisan migrate) isso fará com que a tebela necessária seja criada no banco.

Endpoints

1 - O arquivo de rotas está em routes/api.php
1.1 por causa desse arquivo, as rotas sofrem uma alteração como no exemplo: localhost:8091/api/cliente

Banco de dados

url de acesso: localhost:1010

dados de acesso:

servidor: mysql
user:root
password:root

